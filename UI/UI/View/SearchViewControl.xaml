<UserControl x:Class="Sando.UI.View.SearchViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vsfx="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.10.0"
             xmlns:SearchEngine="clr-namespace:Sando.SearchEngine;assembly=SearchEngine"
             xmlns:local="clr-namespace:Sando.UI" xmlns:view="clr-namespace:Sando.UI.View" mc:Ignorable="d" 
             d:DesignHeight="*" d:DesignWidth="300"
             Name="MyToolWindow"
             Background="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowBackgroundKey}}">    
         
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>            
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.Resources></Grid.Resources>
 
        <StackPanel Margin="10,0,10,10" Grid.Row="0" >
            <StackPanel.Resources>                
                <Style TargetType="{x:Type StackPanel}">
                    <Setter Property="Margin" Value="0,10,0,0"/>
                    
                </Style>
            </StackPanel.Resources>
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="60" />
                </Grid.ColumnDefinitions>
                <TextBox  Grid.Column="0" Height="25" KeyDown="OnKeyDownHandler" BorderThickness="1" BorderBrush="DimGray" Text="{Binding Path=SearchString}"></TextBox>
                <Button Grid.Column="1" Content="{Binding Path=SearchLabel}" HorizontalAlignment="Right" Width="60" Height="25" Name="searchButton" Click="SearchButtonClick" />
            </Grid>
            </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding Path=SearchResults}" ScrollViewer.VerticalScrollBarVisibility="Visible"
                Margin="10,0,10,10" SelectedIndex="0" HorizontalAlignment="Stretch" VerticalContentAlignment="Stretch" MouseDoubleClick="UIElement_OnMouseDown">           
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment"                            
                Value="Stretch" />
                    <Setter Property="Margin" Value="0,0,0,0"/>
                    <Setter Property="Padding" Value="0,0,0,0"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn  Header="Summary" Width="125" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>                                
                                <Grid Margin="0,2,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="20" />
                                    <RowDefinition Height="20" />
                                    <RowDefinition Height="20" />
                                    <RowDefinition Height="0" />
                                 </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock   Text="{Binding Mode=OneWay, Path=Name}" Grid.Row="0" Grid.Column="0" >
                                </TextBlock>
                                <TextBlock   Text="{Binding Mode=OneWay, Path=FileName}" Grid.Row="1" Grid.Column="0" >
                                </TextBlock>
                                <TextBlock  Text="{Binding Mode=OneWay, Path=Parent}" Grid.Row="2" Grid.Column="0" >
                                </TextBlock>
                                <Canvas HorizontalAlignment="Stretch" Height="0" Margin="0,0,0,0" Grid.Row="3" Grid.Column="0">
                                        <Line X1="1" X2="600" Y1="0" Y2="0" Stroke="LightSlateGray" StrokeThickness="1"></Line>
                                </Canvas>
                                </Grid>                
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>                    
                    <GridViewColumn Width="Auto" >
                        <GridViewColumn.Header>
                            <GridViewColumnHeader MinWidth="250" Content="Code Snippet" HorizontalContentAlignment="Left"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical" Margin="0,0,0,0">
                                    <StackPanel Orientation="Horizontal" Margin="-6,0,0,0">
                                        <Canvas Width="1" Height="60" Margin="0,-11,0,0">
                                            <Line X1="1" X2="1" Y1="1" Y2="71" Stroke="LightSlateGray" StrokeThickness="1"></Line>
                                        </Canvas>
                                        <TextBlock Margin="5,2,0,0" FontFamily="Consolas" MinWidth="200" FontSize="9" TextAlignment="Left" VerticalAlignment="Top" Width="Auto"  Height="60" Text="{Binding Mode=OneWay, Path=Snippet}" >
                                        </TextBlock>
                                    </StackPanel>
                                    <Canvas Width="Auto" Height="0" Margin="-5,0,0,0">
                                        <Line X1="1" X2="1000" Y1="0" Y2="0" Stroke="LightSlateGray" StrokeThickness="1"></Line>
                                    </Canvas>
                                </StackPanel>
                            </DataTemplate>                            
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
